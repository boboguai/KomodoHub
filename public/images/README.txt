Local images for species cards, library covers, and reward shop.

- The app now uses local files only (no runtime download).
- Keep file names stable under `/public/images/...` when replacing images.
- Current defaults use `.png` paths such as `/images/species/komodo-dragon.png`.
